dist: xenial
language: python
cache: pip
env:
- TEST_CMD="pytest --cov"
install:
- pip install -r requirements.txt
script:
- $TEST_CMD
after_success:
- coveralls
notifications:
  on_success: change
  on_failure: always
